source 'https://rubygems.org'

# Specify your gem's dependencies in vagrant-ovirt.gemspec
gemspec

group :development do
  # Vagrant no longer supports being used as a gem
  #gem "vagrant", :git => "git://github.com/mitchellh/vagrant.git"
end
